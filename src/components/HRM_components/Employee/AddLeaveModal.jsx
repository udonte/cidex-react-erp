import React from "react";
import Modal from "../_common/ModalContainer/ModalConatiner";

const AddLeaveModal = ({ isOpen }) => {
  return (
    <div>
      <Modal isOpen={isOpen}>dwadsa</Modal>
    </div>
  );
};

export default AddLeaveModal;

export const selectLeadsSlice = (state) => state.leads;
// export const selecteLeadSliceById = (state) => state.lead;

// // lead status
// export const selectLeadStatusSlice = (state) => state.lead_status;
// // export const selecteLeadStatusSliceById = (state) => s;
